<%= form_with(model: picture, local: true, multipart: true) do |f| %>
  <div class="field">
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit  "Upload"%>
  </div>
<% end %>
